{
  "name": "acc-mcp",
  "version": "0.3.0",
  "description": "MCP service for Autodesk Platform Services (APS)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "acc-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "pack": "npm run build && node scripts/pack-mcpb.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@semantic-release/exec": "^7.1.0",
    "@semantic-release/git": "^10.0.1",
    "@types/node": "^20.0.0",
    "semantic-release": "^25.0.3",
    "typescript": "^5.0.0"
  }
}
