# Autodesk Platform Services (APS) credentials
# Create an app at https://aps.autodesk.com/ to get these.
APS_CLIENT_ID=your_client_id
APS_CLIENT_SECRET=your_client_secret
# Optional. Space-separated OAuth scopes (default: data:read). e.g. data:read data:write
APS_SCOPE=data:read
